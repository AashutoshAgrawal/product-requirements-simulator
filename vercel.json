{
  "installCommand": "npm install --prefix ./react-frontend --legacy-peer-deps",
  "buildCommand": "npm run build --prefix ./react-frontend",
  "outputDirectory": "react-frontend/build",
  "framework": "react",
  "env": {
    "CI": "false"
  }
}
